## Evaluación del modelo

<div class="twocol">

<div class="card fragment fade-up accent-left-green pad-14-18" data-fragment-index="1">
<h4 class="text-block-green mb-10" style="font-size: 28px;">Bondad de ajuste</h4>
<table class="table-basic" style="font-size: 28px;">
<thead>
<tr style="border-bottom: 2px solid #1b4d2e; color: #1b4d2e;">
<th class="pad-5">Métrica</th>
<th class="pad-5">Valor</th>
<th class="pad-5">Interpretación</th>
</tr>
</thead>
<tbody>
<tr class="bb-slate200">
<td class="pad-5">AIC</td><td>142.5</td><td>Menor es mejor</td>
</tr>
<tr>
<td class="pad-5">AUC</td><td class="text-green-dark text-bold">0.84</td><td>Muy buena discriminación</td>
</tr>
</tbody>
</table>
<div class="fragment fade-up" data-fragment-index="2" style="margin-top: 10px; background-color: #f0fdf4; border-radius: 6px; padding: 6px 10px;">
<p class="text-center m-0 text-block-green" style="font-size: 28px;">
<strong>Prueba de razón de verosimilitudes:</strong> χ² = 48.7, p < 0.001. El modelo mejora significativamente respecto al nulo.
</p>
</div>
</div>

<div class="card fragment fade-up accent-left-blue pad-14-18" data-fragment-index="3">
<h4 class="text-block-blue mb-10" style="font-size: 22px;">Curva ROC</h4>

```{r}
#| echo: false
#| warning: false
#| message: false
#| fig-width: 4
#| fig-height: 4
#| out-width: "90%"
library(ggplot2)
set.seed(42)
n <- 150
x <- rnorm(n)
prob <- 1 / (1 + exp(-(0.5 + 1.2 * x)))
y <- rbinom(n, 1, prob)
o <- order(prob, decreasing = TRUE)
tpr <- c(0, cumsum(y[o]) / sum(y))
fpr <- c(0, cumsum(1 - y[o]) / sum(1 - y))
auc <- round(sum(diff(fpr) * (tpr[-1] + tpr[-length(tpr)])) / 2, 3)
df_roc <- data.frame(fpr = fpr, tpr = tpr)

ggplot(df_roc, aes(fpr, tpr)) +
  geom_ribbon(aes(ymin = fpr, ymax = tpr), fill = "#dce9f7", alpha = 0.6) +
  geom_abline(slope = 1, intercept = 0, linetype = "dashed",
              color = "#94a3b8", linewidth = 0.9) +
  geom_line(color = "#1a3a6b", linewidth = 1.8) +
  annotate("label", x = 0.62, y = 0.28,
           label = paste0("AUC = ", auc),
           size = 5, color = "#1a3a6b", fill = "#eef6ff",
           fontface = "bold", label.size = 0.5) +
  scale_x_continuous(limits = c(0, 1), expand = c(0.01, 0)) +
  scale_y_continuous(limits = c(0, 1), expand = c(0.01, 0)) +
  labs(x = "Tasa de falsos positivos (1 - Especificidad)",
       y = "Tasa de verdaderos positivos (Sensibilidad)") +
  theme_minimal(base_size = 13) +
  theme(
    axis.title   = element_text(size = 12, color = "#374151"),
    axis.text    = element_text(size = 11, color = "#374151"),
    panel.grid.minor = element_blank(),
    panel.border = element_rect(color = "#cbd5e1", fill = NA, linewidth = 0.5)
  )
```

</div>

</div>


