## Solución

```{r}
x  <- c(0, 1, 2, 3)
px <- c(0.50, 0.30, 0.15, 0.05)
EX  <- sum(x * px)
EX2 <- sum(x^2 * px)
VX  <- EX2 - EX^2
cat("E(X)   =", EX, "\n")
cat("Var(X) =", round(VX, 4), "\n")
cat("sd(X)  =", round(sqrt(VX), 4), "\n")
```

